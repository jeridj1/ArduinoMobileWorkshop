<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Host hardware requirement: the app is unusable without USB Host support. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <application
        android:name=".ArduinoMobileWorkshopApp"
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArduinoMobileWorkshop">
        <activity android:name=".ui.MainActivity" android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.EditorActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.SerialMonitorActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.BoardsManagerActivity" android:exported="false" />
        <activity android:name=".ui.LibraryManagerActivity" android:exported="false" />
        <activity android:name=".ui.SettingsActivity" android:exported="false" />
        <activity android:name=".ui.FilePickerActivity" android:exported="false" />
        <activity android:name=".ui.LogicAnalyzerActivity" android:exported="false" />
        <activity android:name=".ui.MultiProgrammerActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Services live in the rp2040 module; use fully-qualified names. -->
        <service android:name="com.arduinomobileworkshop.rp2040.services.LogicAnalyzerService" android:exported="false" />
        <service android:name="com.arduinomobileworkshop.rp2040.services.RP2040ProgrammerService" android:exported="false" />

        <!--
            Receives system USB attach/detach broadcasts. The intent-filter is
            linked to res/xml/device_filter.xml so the system only wakes the app
            for supported microcontroller vendor IDs.
        -->
        <receiver android:name=".usb.UsbDeviceReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </receiver>
    </application>
</manifest>
